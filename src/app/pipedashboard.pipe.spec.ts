import { PipedashboardPipe } from './pipedashboard.pipe';

describe('PipedashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new PipedashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
